import { AiOutlineArrowLeft } from "react-icons/ai";
const Arrow2 = ({ handleClick, className }) => {
    return <AiOutlineArrowLeft onClick={handleClick} className={className}></AiOutlineArrowLeft>;
  };

export default Arrow2;
